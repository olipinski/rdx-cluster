---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: docker-mailserver-app
  namespace: flux-system
spec:
  interval: 30m
  targetNamespace: docker-mailserver
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/apps/docker-mailserver/app/overlays/prod
  prune: true
  dependsOn:
    - name: cert-manager-app
    - name: cilium-app
    - name: cloudnative-pg-app
    - name: external-dns-app
    - name: external-secrets-app
  wait: true
  healthChecks:
    - apiVersion: helm.toolkit.fluxcd.io/v2
      kind: HelmRelease
      name: docker-mailserver
      namespace: docker-mailserver
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
      - kind: ConfigMap
        name: domain-settings
      - kind: Secret
        name: docker-mailserver-secret
