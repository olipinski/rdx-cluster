---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: openbao-app
  namespace: flux-system
spec:
  interval: 30m
  targetNamespace: openbao
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./kubernetes/platform/openbao/app/overlays/prod
  prune: true
  wait: true
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
      - kind: ConfigMap
        name: domain-settings
