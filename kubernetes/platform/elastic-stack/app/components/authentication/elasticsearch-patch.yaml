---
- op: add
  path: /spec/auth
  value:
    roles:
      - secretName: es-opentelemetry-roles-secret
      - secretName: es-prometheus-roles-secret
    fileRealm:
      - secretName: es-admin-user-file-realm
      - secretName: es-opentelemetry-user-file-realm
      - secretName: es-prometheus-user-file-realm
