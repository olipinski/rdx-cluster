---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloadb-s3-externalsecret
  namespace: keycloak
spec:
  secretStoreRef:
    name: openbao-backend
    kind: ClusterSecretStore
  target:
    name: keycloak-db-s3-secret
    template:
      engineVersion: v2
      data:
        AWS_ENDPOINTS: "https://${S3_SERVER}:8443"
        AWS_ACCESS_KEY_ID: "{{ .user | toString }}"
        AWS_SECRET_ACCESS_KEY: "{{ .key | toString }}"
  data:
    - secretKey: user
      remoteRef:
        key: seaweedfs/barman
        property: user
    - secretKey: key
      remoteRef:
        key: seaweedfs/barman
        property: key
