# flux-instance helm values (dev overlay)
instance:
  sync:
    kind: GitRepository
    url: https://github.com/olipinski/rdx-cluster
    ref: refs/heads/master
    path: kubernetes/cluster/dev
    interval: 1h
