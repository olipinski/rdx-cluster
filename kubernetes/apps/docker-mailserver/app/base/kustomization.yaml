apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ns.yaml
  - docker-mailserver-externalsecret.yaml
  - helm.yaml

configMapGenerator:
  - name: docker-mailserver-helm-values
    files:
      - base-values.yaml=values.yaml

configurations:
  - kustomizeconfig.yaml