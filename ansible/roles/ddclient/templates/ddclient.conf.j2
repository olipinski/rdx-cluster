# General config
daemon={{ run_interval }}
ssl=yes

# Router
use=web, web=ipify-ipv4

# Protocol
protocol=cloudflare
zone={{ cloudflare_zone }}
ttl=60
login=token
password={{ cloudflare_token }}
{{ ddns_domain }}